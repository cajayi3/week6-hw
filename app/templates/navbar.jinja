<nav class="navbar navbar-expand-lg bg-body-tertiary mt-2">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Dog Tag</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
     aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{ url_for('ranger.home') }}">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ url_for('ranger.colonel') }}">Colonel</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ url_for('marine.major') }}">Major</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ url_for('marine.sealsix') }}">Sealsix</a>
        </li>
        {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{ url_for('copy.user_page', username=current_user.username) }}">My Page</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ url_for('copy.post') }}">Post</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ url_for('marine_logout') }}">Logout</a>
        </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>