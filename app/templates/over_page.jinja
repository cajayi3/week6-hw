{% extends "lieutenant.jinja" %}

{% block content %}
<h3>{{ over.username }}'s Page</h3>

{% for post in user.posts %}
  <p>{{ post.body }}</p>
  <small>{{ post.timestamp }}</small>
{% endfor %}
{% endblock %}